import 'package:flutter/material.dart';

class RouteDoisPage extends StatelessWidget {
  const RouteDoisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rota Dois')),
      body: const Center(child: Text('Página Dois')),
    );
  }
}
