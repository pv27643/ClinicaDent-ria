import 'package:flutter/material.dart';

class RouteTresPage extends StatelessWidget {
  const RouteTresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rota Três')),
      body: const Center(child: Text('Página Três')),
    );
  }
}
